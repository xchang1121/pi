# 更新记录

## 当前实现

本文维护当前行为与重要边界，不累计已经撤销的功能方案和版本叙述；历史改动保存在 Git 提交记录中。

### 动作身份与预测

- Drafter、Actor probe 和 PatternAware 共用计划、调度与结算。参数在生成 K(a) 前准备一次，键与实际执行器绑定后不再漂移。
- K(a) 包含语义、schema、已准备参数和执行身份；资源新鲜度由封存证据独立证明。预测命中要求请求覆盖，仅复用输入不能算命中；来源、调用编号和轮次不构成执行等价性。
- 计划先捕获不可变输入、再按修订准入；父语义变化重绑后代，Actor 借用保留结算权。查询先筛节点，再读取当前依赖状态。
- 候选索引保存注册成员与投影规则，回调不能复活退休记录。冷热缓存共用一次收益排序处理缩容与降级；借用项仍占预算，同键不同输入版本各自保留。
- Drafter 默认两个单动作请求，首个完成参数校验和执行绑定的有效动作才可取消同伴；迟到绑定不再进入候选回调。后继沿用下一决策额度；请求成本与跨轮次收益更新所属滚动样本，未发请求不记样本，退休样本不复活。
- Actor probe 只派生自权威流，与 Drafter 共用请求及采纳预算；命中后无法观测的收益保持未知，不记零或强制 Actor 重跑。历史回退耗时仅作参考，不能冒充关闭投机的基线；执行领先与目标 token 验收分别计量。
- PatternAware 每个前沿共享有界后缀概率，每组共享间距分布、分位数与加权累加；保持预测字段和排序一致。零阶不遍历历史，只恢复当前索引和一致样本，不匹配格式不改写文件。
- 工具选择只保留字符串数组；移除旧三分组迁移、辅助函数及兼容类型导出。缺省使用默认列表，无效选择关闭工具预测，有效数组保持去重和注册工具过滤；选择范围不授予执行权限。

### 执行、证据与事务

- Router 统一选择执行环境，K(a) 不随隔离路线改变；ActorAction 统一候选选择、准入释放与权威结算，选中后禁止回退或迟到拒绝。
- 通用工具错误封装保留原文本格式，移除仅为异常转字符串而加载的模型库入口，降低独立 runner 启动成本。
- 完整流式意图提前转换封存数据，与 Actor 生成重叠；正式调用已到达则跳过准备。结果按完整 K(a) 共用原预算与回收，采纳仍重新授权、验证与提交。无动作扩张或参数补齐；输出裁剪仅由显式适配器提供。
- 资源指纹批次失败后停止领取新项，等待已启动读取和文件句柄关闭，再返回原始错误；递归目录与平铺依赖共用该路径。
- 所有者关闭先停止准入，排空执行、输入、事务与清理；并发 close 共用完成信号。文件工作区记录输入与效果，同锁验证后提交，省去 Git 扫描；新目录保留 Actor 原生继承策略，新建后观察权限未经证明则回退。操作错误、取消或未排空不能发布。
- 原生 helper 保留父退出状态并等待全部后代；helper/strace 被杀时终止所有 tracee，包含独立进程组。strace 缺失或截断的退出记录禁止封存；旧 helper 与旧证书不满足当前资格。
- 可执行路径按有界批次准备；Sandlock 优先借助 O_PATH 描述符解析完整路径，失败沿用原路径解析，不缓存可变路径身份。ELF 分类仍不消耗脚本输入，Runtime 与安装器保留真实脚本资格检查。
- `createExecPrototype` 返回已校验且自有的规范原型。进程消费者接收在调用边界生成一次的 `weakKey`，不重复规范化，也不把键当作执行许可。
- 父分支与子进程共享一次性消费权，防止同一随机结果被整体和子进程重复采纳；仅明确回滚成功才恢复资格。可重复证书仍可复用，重放前持有完整输出/效果闭包。
- 原子发布只回收成功独占创建的临时文件；GC 与失败的并行维护必须由原有执行生命周期等待。
- 共享事务和投影结果持有独立副本，保留可枚举 Symbol 数据。无法证明共享性的值拒绝复用，不改写 Actor 权威结果。
- 采纳验证等价性、权限、新鲜度与生产者保证；准入比较剩余成本，首次转换与缓存查询分别采样。未知 Actor 耗时不设零等待预算，稀疏或宽泛样本不否决已完成候选。干净拒绝允许一次 fallback；不确定或部分提交失败禁止重跑。

### 搜索与界面

- Native Pi 是默认搜索方式；TUI 的 Captured search 显式共同绑定 Actor 与投机执行器。
- 受控 find 复用 Pi 已有匹配/忽略组件；grep 只使用已验收的现有 rg，不新增安装或下载。缺少 rg 不影响受控 find。
- 搜索输入包含被消费的原始字节、具名祖先/Git 配置、正负元数据和 HOME 身份；原版 Pi 负责完整输出、上下文和截断。
- 该 profile 明确排序并禁用环境 rg/全局 ignore 配置，不宣称等价于原生默认。绑定失败不暗中换回另一执行器。
- TUI 区分预测、重放、观察与提前执行能力；设置在 Apply 后生效，刷新与关闭等待真实操作完成。
- 根 README 是唯一中文使用入口；基准说明单独描述低负载验收、性能测试和平台限制。

### 验证边界

- 当前基线与复现条件见[验收说明](docs/bash-reuse-capability-lattice.md#当前证据与剩余工作)；Windows/WSL 的完整测试、性能测量和体积分别记录，不沿用旧提交的统计。
- 任务计时包含被采纳的查询转换，按实际计算去重，跨任务不重计；准备后被拒绝不算权威工作。基准包含初始化与回收，加速比为反事实串行总时长/端到端时长。构造流留仓库外，付费 API 为零，不作自然模型性能证据。
- Windows x64 与 WSL Linux x64 的受控搜索及脚本驱动 TUI 已验收；性能测量与普通回归分开，不把候选就绪等同于有收益。
- Linux Bash 保留整体、子进程和运行中复用。缺少 Landlock 时仅能消费可接受的既有证书；独立 Actor 未命中证书生产仍未完成。
- ThinkThread alpha4 x86_64 已验收部分文件采纳、冲突、取消、回退和 TUI；新文件 write、嵌套 Bash、自然任务净加速、macOS/ARM64 及历史代码预算仍未验收完成。
